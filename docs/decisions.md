# Decisions

Choices a test cannot express: canonical identifiers, defaults, naming. One
row each, decided **before** code builds on them (see workspace `AGENTS.md`,
"Spec before code"). A decided row records the decision and the law/test it
materialized into; nothing here carries a count or a status narrative.

| # | Question | Decision | Materialized as |
|---|---|---|---|
| D-1 | Canonical automation identifier — config `id`, alias, or `entity_id`? `--pattern` rejects the config id `auto show` prints (R2/T10). | DECIDED 2026-07-27: accept all three everywhere (H-17's pole); config `id` is the canonical printed form | `TestAutomationRefSurfaceIsClosed` (autoref surface — every automation-target entrypoint reaches `resolveAutomation`), `TestPatternAcceptsEveryIdentifierHactlPrints` (`make test-int`), the `…AcceptsTheAliasHactlPrints` pair, `TestAutoRollbackAcceptsTheIdentifierAutoLsPrints`, `TestAutoDeleteSendsTheCompanionAnIdItAccepts`; law: H-17 |
| D-2 | Filter case pole — all four `--pattern` flags are case-sensitive while every `--name`/`--area`/`--label` sibling is not. | DECIDED 2026-07-27: all filters case-insensitive | `TestFilterFlagsAgreeOnCase` (internal/cmd/surface_filter_test.go) — asserts the case-insensitive pole over every filter probe, not parity |
| D-3 | `dash show` default view (R17/D67) — the default dashboard is often auto-generated, so "views summary" is unanswerable. | DECIDED 2026-07-27: honest report — views summary when stored config exists; when auto-generated, say exactly that and point to `dash ls`. Never fabricate a strategy render. | `classifyDefaultDashboard` + `TestDashShowDefault{,JSON,Raw}`; oracle: `TestOracleLovelaceDefaultDashboardStates` (`make test-int`) |
| D-4 | Actor answer — `ent show`'s `changed_by` (state context) vs `ent who` (logbook) disagree for logbook-excluded entities (R20/D70). | DECIDED 2026-07-27: converge on one shared resolution — logbook first, context fallback; every answer names its source, and `ent who` states explicitly when the logbook excludes the entity. | `resolveActor` (internal/cmd/whoresolve.go) + `TestActorConverges_*` (actor_convergence_test.go); exclusion semantics cited from HA 2026.7.2 source |
| D-5 | `anom:` identifiers — minted by `ent anomalies`, consumed by nothing (R19/D69). | DECIDED 2026-07-27: delete the minting (H-11's spirit: no identifier without a consumer). Re-minting must arrive in the same PR as its consumer. | minting already deleted by `5c57180` (v2026.7.12); standing gate `TestEntAnomaliesMintsNoIdentifier` keeps it deleted |
| D-6 | `ref replace` and the default dashboard — vacuous storage-mode check skips it silently, every run (D71). | DECIDED 2026-07-27: fix classification — attempt `lovelace/config` for the default dashboard and include it when retrievable; a genuinely YAML-mode default is a loud partial-scan failure unless `--allow-partial` (mirrors `ref validate`). | `classifyDefaultDashboard` in `ref replace` + partial gates (`ref_test.go`); the vacuous `lovelace/info` check and its client are deleted; yaml oracle: `TestOracleLovelaceYamlModeDefault` |
| D-7 | `ref validate --exit-code` silently skips a dashboard whose config fetch fails — the unfixed half of P1-8, and D-6's vacuous-gate shape on the one command whose whole job is to certify a tree. | DECIDED 2026-07-28: loud partial, gated on who reads the answer. A dashboard that could not be scanned refuses the run under `--exit-code` **or** `--json` unless `--allow-partial` — a warning is invisible to a machine consumer, and H-10 makes `--json` a machine contract, so an unread dashboard must not reach one as a clean verdict. Plain text states the partial scope in the report body, never only in a log. An auto-generated default is a complete zero, not a partial. Search commands (`ref scan`, `dash grep`) report what they could not read and still answer. **Extended 2026-07-28, same day, after a verification pass: the rule is per _source_, not per dashboard.** Stating it in terms of dashboards is how it shipped one source short — `ref validate` reads four (entity registry, live states, config files, every dashboard) and the registry degraded the live set at `slog.Warn` and reached no gate, so the fix for a silent dashboard left a silent registry one function beneath it. All four are now recorded in one `validateScope` and reported by one function; three of them (registry, config files, dashboards) then share `validateScanGateError`, while live states refuse unconditionally from their own branch in `liveEntitySet` — a different message for a different consequence, not a posture flag on the shared one. Both directions are gated: an unread config file or dashboard hides references (false clean bill), a degraded live set reports existing entities as dangling (false alarm). Live states keep the stricter posture — refuse in every mode — because the registry alone omits every state-only entity and is not a usable live set. **Known limit, recorded rather than implied:** the "config files" source is one wire call over N files, and the companion silently skips a file it cannot read (missing `!include` target, unreadable, circular). hactl cannot see those skips until it consumes the `skipped` array (hactl-companion #81) after a companion release, so this row does not yet claim the config half is gated file-by-file. | one `walkDashboardConfigs` shared by `ref validate`, `ref replace`, `ref scan` and `dash grep` (three walks collapsed into one, so the class has one site rather than a gate over two) + one `validateScanGateError` over three sources plus an unconditional refusal for live states, all four carried in `validateScope`; `TestRunRefValidate_UnscannableDashboardRefusesUnlessAllowPartial`, `TestRunRefValidate_UnreadableRegistryRefusesUnlessAllowPartial`, `TestRunRefValidate_UnreadableConfigHalfRefusesUnlessAllowPartial`, `TestRunRefValidate_AutoGeneratedDefaultIsNotAPartialSweep`, `TestRunRefScan_UnscannableDashboardStillAnswers`, `TestRunRefValidate_RegistryOnlyRefusedWithoutAllowPartial`; laws: H-10 |
| D-8 | `auto ls`/`script ls` die on an entity they do not list — a foreign `current: -1.7525` fails a decode of the whole `/api/states` into their own domain-typed struct. Is the pole "attributes are schema-tolerant" or "filter before decode"? | DECIDED 2026-07-29: the **ordering** rule (H-21). A command may keep a typed attribute schema for the entities it actually renders; what it may not do is apply that schema to entities it discards. Widening `Current` to a tolerant numeric type is explicitly **not** the fix — it addresses one key, leaves the next collision to production, and would make the reproduction pass for the wrong reason. The oracle settled the companion question: HA computes `current` as `Script.runs` → `return len(self._runs)`, integral by construction, so the field is correct for every entity the listing renders. **The collision ships in stock HA, not in exotic integrations** — `max` is integral on `automation.*`/`script.*` and fractional on `number.*` in the same response, so `automationAttributes` is one `Max int` field away from failing on a default install. The current pole was held by luck, not design. Marker 1 ("which entity emitted `-1.7525`") was **retired by scope, not probed**: the reporting instance is a third party's and unreachable, and a different answer would have produced neither different code nor a different test — it would have produced a *narrower* fixture than acceptance criterion 1 already demands. That discriminator is now ritual, in the workspace `AGENTS.md` "Spec before code" item 1. | `internal/cmd/states.go` (`statesEnvelope` + `fetchDomainStates` + `decodeStateAttributes`); `TestAutoLsIgnoresAttributesOfEntitiesItDiscards`, `TestScriptLsIgnoresAttributesOfEntitiesItDiscards`, `TestColliderCoversEachStructsOwnKeys`, `TestStatesWithoutEntityIDStillPoisonsTheListing`, `TestAutoLsIgnoresAForeignEntitysAttributeTypes`, `TestScriptLsIgnoresAForeignEntitysAttributeTypes`, `TestEntListingsStillSeeTheColliderIsTheControl`; oracles `TestOracleStatesCarriesOneKeyAtTwoJSONTypes`, `TestOracleAutomationScriptCurrentIsIntegral`, `TestOracleStatesSixKeyDomainCensus`; closure gate `TestDomainDecodeSurfaceIsClosed` over `dev/surfaces/domaindecode.manifest`; law: H-21 |
| D-12 | Energy access — a curated command or a generic WS passthrough? The field report behind #114 stalled because `energy/get_prefs` was unreachable, and every future "hactl can't do X over WS" costs a session the same way; an escape hatch (`hactl ws get <type>`) would end that class at once. | DECIDED 2026-07-29 (Jan): **curated only, no passthrough** — the manual stays the contract LLM callers execute and `degeneracy` stays in the loop; the answer to an unreachable WS command is a new curated command, not a hole in the contract. Naming: `hactl energy show` (noun family + `show`, consistent with `dash show`; room for curated siblings). Recorder statistics deliberately OUT this round, decided the same day — do not re-triage; `recorder.purge` stays reachable via `svc call`. | `hactl energy show` (internal/cmd/energy.go, `EnergyGetPrefs` in internal/haapi/energy.go); non-goal recorded here and in SPEC-energy.md (archived); oracle: `TestOracleEnergyGetPrefsUnconfigured` |
| D-11 | `ent rename` composition — which half runs first, and what a half-failure says. The registry rename (`config/entity_registry/update` + `new_entity_id`) and the reference rewrite (`ref replace`) are two writes with no transaction around them. | DECIDED 2026-07-30: registry first — it is the authoritative object, and a failed rename leaves every reference untouched (clean abort). A failed rewrite **after** a successful rename does not roll back: the error names the completed half and prints the exact remediation (`hactl ref replace <old> <new> --confirm`, idempotent). The dry run previews one plan carrying both halves (old, new, reference count from a read-only scan); partial-scan posture inherits D-6/D-7 via `--allow-partial`. Collisions are pre-checked against the registry AND HA is still trusted to refuse on a race (oracle-verified that it does). Cross-domain renames pass HA's verdict through — same posture as helper delete's confirm-time 409. | `runEntRename` (ent.go) + `TestEntRenameDryRunRefusesUnresolvable` (collision + unknown-old + state-only refusals), `TestOracleEntityRenameSemantics` (rename premise, server-side collision refusal, old id freed, state-only refusal), `TestE2EEntRenameCLI` (H-12, make test-companion); law: H-2, H-17 |
| D-10 | `dash grep`/`ref scan` matching poles, and what a miss message may claim. The two commands match differently (dashboards: whole string values via jsonwalk equality; config half: word-boundary token via the companion's `_target_pattern`), the manual claimed they matched the same, and both miss messages ("not referenced …") read as verified negatives for substring intents they never tested — a field agent took "not referenced in any dashboard" as fact while `sensor.balkony_p_ac` sat in two dashboards. | DECIDED 2026-07-29: both poles stay as they are and are documented as themselves, never as each other. Substring/free-text search is deliberately **not** bolted on here — term discovery is `ent ls --pattern`/the name search today, and a real forward grep is its own epic (hemm-ems/hactl#115) with a wire-contract change. A miss message may only claim what the query tested: both misses now name their matching contract and route term discovery to `ent ls --pattern`. | miss messages in `runDashGrep` (dash.go) and `runRefScan` (ref.go) + `TestRunDashGrep_NoReferences`/`TestRunRefScan_NoReferences` (both pin the contract phrase and the routing); manual "References" section states the two-pole asymmetry; `target.manifest` exempt rows quote the new phrasing |
| D-9 | A defect reported from an instance nobody here can reach cost a source-reading session to diagnose, because the error named hactl's Go type and not the entity. What diagnostic mechanism answers that class — a `--debug` flag producing a dump? | DECIDED 2026-07-29: **no `--debug` flag.** It needs the user to know to re-run, needs the failure to reproduce, and the failing run already held the payload. Three layers instead, and only the first ships now. **Layer 0 — the error names the record** (`entity <id>: attributes.<key>: …`): in scope, because it *manufactures oracles* — every future report of this class arrives with its own diagnosis inside it. Note it needs more than a per-entity loop: the message is reconstructed from `*json.UnmarshalTypeError`'s `Field`/`Value`/`Type`, since decoding per entity still yields encoding/json's Go-type-centric phrasing. **Layer 2 — `hactl doctor`, scoped to a redacted wire shape census**: deferred to its own item; capture shapes, not values, because `/api/states` is a description of someone's home and a raw dump attached to a public issue is a privacy incident — and a shape record is mechanically convertible into a fixture, closing the report → oracle → red test → fix loop. **Layer 1 — automatic capture on error: dropped**, not deferred; once layers 0 and 2 exist its only remaining value is a transient unreproducible failure, at the highest machinery cost of the three. | `attributeDecodeDetail` in `internal/cmd/states.go` + `TestStatesDecodeErrorNamesTheEntityAndKey`; layer 2 unbuilt by design; law: H-21 |
| D-13 | `area create ""` created a real area with a blank `area_id` and took down every `area` command (ls/create/delete) on a production instance until a raw WebSocket delete removed it. The field report's own recommendation was to make the registry list decoder tolerant — skip the identity-less record and warn. | DECIDED 2026-07-30: **refuse the create, do not soften the decoder.** A tolerant listing is H-14 inverted: the whole point of the poison is that a row silently missing from an answer is worse than a loud failure, and "skip+warn" is precisely the shape (`slog.Warn` into a stream `HACTL_LOG_LEVEL` routinely hides) that D-7 had to undo one function at a time. The record must not exist in the first place, and it can only be prevented client-side — the oracle shows HA accepts a blank name, mints a blank id, and files a whitespace-only name under an id it chose ("unknown"), all in one round trip. What the degenerate path gets instead is a **way out**: the error keeps its diagnosis and adds the recovery (delete in HA's UI, or `config/<kind>_registry/delete` with an empty id), because the one thing hactl genuinely cannot do here is clean up after itself. Blank means "no non-whitespace character" — HA's own uniqueness check normalises whitespace, so a narrower rule would disagree with the server about what a name is; trimming the caller's name is explicitly NOT done. | `requireRegistryName` (internal/cmd/registry_name.go) on all three creates + `withRegistryRecovery` (internal/haapi/registry_recovery.go) on all three listings; `TestRegistryCreateRefusesABlankName`, `TestRegistryCreateAcceptsANameWithSurroundingSpace`, `TestRegistryListBlankIdentityErrorPointsAtRecovery`, `TestRegistryCreateBlankNameChangesNothingInHA` (`make test-int`); oracle `TestOracleRegistryCreateAcceptsABlankName`; laws: H-2, H-14 |
| D-14 | Config writes and formatting — a single-entry write re-serialized the whole file. Is byte preservation a goal at all, and if it is, where does the fix stop? (live-fire 2026-07-30 P1 #4: one confirmed automation write reformatted ~34 unrelated real automations; semantically lossless, so every value-level test stayed green) | DECIDED 2026-07-30: **byte preservation is a contract, not a nicety** — a write that changes one entry changes one entry's lines and leaves every other byte alone, because a config directory is a git repo and a diff nobody asked for is the whole cost. Two consequences settle the rest. (a) **The check is on bytes, never on parsed values**: the defect *was* semantically lossless, so a parse-and-compare gate would have certified it clean — which is exactly what happened for months. (b) **The fallback is loud**: a file the splice cannot carry (an anchor defined in the replaced entry, a flow-style top level) is re-serialized whole and the response says `reformatted: true`, absent otherwise — logging it instead would repeat the `ServiceResult` mistake of knowing the reason and telling nobody. **Scope, recorded rather than implied**: the fix is *entry*-granular, so it covers `POST/PUT/DELETE /v1/config/{automation,script,template,helper}` and nothing else. `POST /v1/ref/replace` is *leaf*-granular (scalars at arbitrary depth across a whole `!include` graph) and keeps re-serializing — so `ent rename` still reformats — hactl-companion #88. `auto apply`/`auto rollback` post to HA's own `/api/config/automation/config/<id>`, which re-dumps the file itself; unfixable in the companion and rerouted from hactl instead — #128, **done 2026-07-31: `internal/writer` writes through `PUT /v1/config/automation` and there is deliberately no fallback (`TestAutoWritesRefuseWithoutACompanion`)**. Both are enumerated with a written reason in the companion's `FILE_WRITES` table, not left as gaps. | hactl-companion `companion/surgical.py` (line splice + a re-parse-and-compare verification that falls back to the old whole-file dump on any disagreement, so the new path can change formatting and never content); companion `INVARIANTS.md` C-13 + `test_single_entry_write_leaves_every_other_byte_alone` (sweeps every write route, with an anti-vacuity guard that re-runs the old writer on the same input) + `test_every_file_write_declares_a_formatting_stance` (canary: `surgical` or a `whole_file_reason`); `tests/test_surgical.py`; oracle `TestSurgicalWrites::test_home_assistants_own_config_api_reserializes_the_whole_file` (HA's behaviour asserted, so the boundary claim fails instead of becoming folklore); owed here: `make sync-spec` + surfacing `reformatted` |
| D-15 | The `--json` machine contract on a write. A preview honoured `--json` and the confirmed run did not, in fourteen commands: is the fix "make each one print JSON", or is there a pole? And what makes a document a document, given `--json` is exempt from `--tokensmax` and `dash show --raw` is not? | DECIDED 2026-07-30: **one pole for both halves — output whose contract is "this parses" is answered whole, on every branch.** (a) A confirmed write renders through `done()`, `dryRunPlan`'s counterpart, carrying `dry_run:false`, `ok`, `action`, `details` and `warnings`; the human text is passed in verbatim so no existing message changes. Valid JSON is deliberately **not** the bar: `config delete` already echoed HA's `{"require_restart":false}`, which parses and names neither the act nor its outcome, so a caller still could not tell it from a preview. (b) A rendered wall clock never reaches a machine: JSON carries the full instant with its offset (`clock.ISO`), the table keeps the short form, and the one place they diverge is `format.Table.SetMachine`. The gate is written against the value's SHAPE, not against a list of timestamp-ish field names — `first_seen`/`last_seen` is exactly the column a name list forgets. (c) An attribute map keeps its wire number form (`json.Number` on the `wireAttributes` type, not on one renderer), so `5000.0` does not become `5000`. (d) The token cap never applies to a document — `--raw`, `--yaml`, `--view`, the verbatim `cat` family, `config file|block`, `completion <shell>` all declare themselves with `markStructuredOutput`; failing loudly instead was rejected, because the command *can* answer and the caller asked it to, and because `--json` had already set the opposite precedent. Prose stays capped, and a test holds that. | `internal/cmd/result.go` (`done`/`writeResult`), `clock.ISO` + `format.Table.SetMachine`, `wireAttributes` in `internal/cmd/ent.go`, `markStructuredOutput` in `internal/cmd/root.go`; gates `TestJSONConfirmContract`, `TestJSONContract` clause (4), `TestDocumentOutputIsNeverTokenCapped` + `TestProseOutputIsStillTokenCapped`, `TestVerbatimLeavesAreDispositionedForTheTokenCap`; closure gate `TestResultSurfaceIsClosed` over `dev/surfaces/result.manifest`; oracle `TestOracleEntShowJSONPreservesWireNumberForm`; law: H-10 |
| D-16 | `energy show`'s `direction` vocabulary. Fixing finding #26 (grid import and gas consumption both labelled "production") means deciding what each source type's two statistic fields are called — and the three words in use (`consumption`/`production`/`return`) have no honest slot for a battery, which neither consumes nor generates. | DECIDED 2026-07-31: **direction is a property of (source type, field), and the vocabulary follows HA's own words rather than a fixed triple.** Grid `consumption`/`return`, solar `production`, gas and water `consumption` — each taken from the docstring on the matching TypedDict in `homeassistant/components/energy/data.py` ("Import meter - kWh consumed from grid", "the source of gas consumption"), never from reasoning about what `stat_energy_from` ought to mean. Battery gets two new words, `discharge` and `charge`: calling a discharging battery "production" would assert that a battery generates energy, which is the same confident wrongness the fix removes, and HA's own comment is "positive when discharging, negative when charging". An unrecognised source type reads `unknown` and still shows its statistic (H-14's spirit — a value that did not resolve is spelled out, never rendered as an answer); it is not an error, because the other rows are still answers. **No new law was minted.** The rule "a derived label is derived from every field that determines it" is real but its enforcement set — every function mapping wire values to a fixed vocabulary — cannot be derived mechanically from the tree, and a law whose gate is a hand-list is the shape `dev/surfaces/README.md` exists to refuse. The closure here is narrower and genuinely complete instead: one table over HA's closed `SourceType` union, plus an oracle that reads the union out of the running container's own `data.py` and fails the build when it gains a member. | `energyDirectionByType` + `energyDirection` in `internal/cmd/energy.go` (both the flat and the legacy `flow_from`/`flow_to` arms read the one table, so the two forms of a grid source cannot disagree again); `TestEnergyDirectionIsAPropertyOfTheSourceType`, `TestEnergyUnknownSourceTypeIsNotGuessedAt`, `TestEnergyLegacyGridFormReadsTheSameTable`; closure gate `TestOracleEnergySourceTypes` (`make test-int`, reads `data.py` from the running image); sweep case `TestSweepEnergyDirectionFollowsTheSourceType` (both profiles); rig shape R4 (`.storage/energy`, all five types, seeded legacy so HA migrates it) |
| D-17 | `--full` means "show full/raw output" in every command's help screen and reached exactly one cap, the table's `--top`. Live-fire #21: `config entries --full` dropped the row cap, produced 213 rows, and was then cut by the untouched 500-token prose cap at SEVEN — three rows FEWER than the same command without the flag, the last one severed mid-field — while `config show <entry> --full` on an entry big enough to truncate was byte-identical to `config show <entry>`. Is the fix per-command wiring, or is there a pole? | DECIDED 2026-07-31: **one rule — `--full` lifts every cap on the answer's size**, the `--top` row cap and the `--tokensmax` token cap alike. That is the only reading of "full" both symptoms satisfy at once, and a flag meaning something different in each of sixty commands is the defect rather than the fix. **An explicit `--tokensmax` still wins**: `--full --tokensmax 200` is a caller asking for every row and a bound on the bytes, and no reading of a flag justifies discarding a number the caller typed — which is why this is not "`--full` implies `--tokensmax=0`". Cobra's `Changed` is the discriminator, and it is reset per invocation alongside the flag values: it lives on the flag object, so one `--tokensmax` would otherwise look typed to every later in-process run, and the MCP server runs every tool call that way. **No law minted here**: the general flag contract is WP10's, and half a law with one flag under it is worse than none — the sweep that would enforce it already exists. | `effectiveTokensMax`/`tokensMaxWasGiven` in `internal/cmd/root.go` (one site, read by `applyTokenPolicy`, so no command opts in or out); `TestFullNeverTruncates` (sweeps the live cobra tree via TestJSONContract's own fixture, classification and positional table, and refuses to pass vacuously — it fails if no swept command's `--full` answer exceeds the cap), `TestFullLiftsTheTokenCap` (the reported arithmetic, including the count that went down, the explicit-cap override, and the stickiness); fixture: `contractConfigEntries` = 40, because a one-row listing cannot tell a working `--full` from a broken one; sweep case `TestSweepFullShowsMoreNotLess` (both profiles) |
| D-18 | What a failure message is allowed to say, and what it owes the reader. Three live-fire findings are one question: every companion error printed the full request path, which under Ingress carries the add-on's Supervisor token (#23); an explicit `--dir`/`$HACTL_DIR` that did not resolve printed the generic four-step discovery block instead of the path it was handed (#55/#77); and `config block template.yaml <unique_id>` answered `Block not found`, word for word what a typo gets, while its own `--help` promised `tpl cat <unique_id>` (#24). | DECIDED 2026-07-31: **an error names what the reader needs to act and nothing about how the request travelled.** (a) The *route*, not the URL: derived by trimming the base URL's own path, so any transport prefix — Ingress, a reverse proxy, whatever is next — is handled by one rule rather than a match on `hassio_ingress`. net/http's `*url.Error` embeds the whole URL in its own message, so the scrub happens in `doOnce` where the error is born; wrapping a clean route around a dirty one would have printed both. **`companion status` still prints the full URL** — that command's question IS the transport and a user debugging discovery needs the address to curl — and that asymmetry is deliberate, recorded here so it is not "harmonised" away later. (b) The *path the caller gave*, when the caller gave one: `--dir`/`$HACTL_DIR` are explicit and are repeated back with their source (`no .env at <path> (from --dir)`); discovery names nothing because the caller named nothing, and the four-step block it keeps already lists every candidate tried. The oracle for the wording was the sibling one branch down, `no HA_TOKEN in .env at <path>` — the divergence was between two errors from one function. (c) The *command that can answer*, when one exists: `config block` asks the companion whether the id resolves as a template rather than branching on the filename, so a template split into its own file is routed and a typo inside `template.yaml` is not. A failure that says nothing about the id (a 5xx) never earns the referral — the probe runs only on a typed 404. | (a) `Client.route` + `Client.scrubTransportError` in `internal/companion/client.go`, and `haapi.NewHTTPStatusError` exported so a companion failure is the same typed error an HA failure is (a caller asking "was that a 404?" no longer matches on message text); `TestErrorNamesTheRouteNotTheURL`, `TestTransportErrorNamesTheRouteToo`, `TestE2ECompanionErrorNamesTheRouteCLI`, sweep case `TestSweepCompanionErrorsNameTheRouteNotTheURL` (live; the rig has no companion — debt R11). (b) `origin` + `withOrigin` in `internal/config/config.go`; `TestLoad_ExplicitDirNamesThePathItTried`, which also pins the sibling it was harmonised toward, plus `TestSweepExplicitDirNamesThePathItTried` (both profiles). (c) `templateRedirect` in `internal/cmd/flow.go`; `TestConfigBlockRedirectsToTplCat`, `TestConfigBlockDoesNotRedirectOnANonNotFound`, `TestE2EConfigBlockRoutesATemplateUniqueIDCLI`, sweep case `TestSweepConfigBlockRoutesATemplateUniqueID` (discovers the id from the instance's own `template.yaml`, because an id that exists on one house is not a property) |
| D-19 | Where a display shortening lives, and who owes an unknown component name an error. Two live-fire findings are one question about who a value is being rendered for. `--full` reached no message column at all: six functions in five files each cut a string while assembling a `format.Table` row, so `--json`, `--full` and `--tokensmax 0` were all downstream of the cut (#14; five of the six were unreported, two of them reaching a machine). And `cc logs totally_bogus_xyz` answered "no log entries for totally_bogus_xyz" at exit 0 — byte for byte what a real, installed, error-free component gets — while its sibling `cc show` refuses the same name at exit 1 (#18). | DECIDED 2026-07-31: **(a) a value is shortened by the renderer, never on the way in.** A column too wide to print declares a width (`format.Table.SetWidth`), which only `renderText` consults; `--full` lifts it, because that is what the flag's own help says. This is not "make `--json` re-fetch the value" — the cut was simply happening one layer too early, and the same three lines were also wrong in two ways nobody reported: they tested LENGTH, so a message whose first line was short kept its newline and split a table row (58 lines for 54 rows on the reference instance), and they sliced BYTES, so a two-byte character on the cut left invalid UTF-8 in `--json`. One implementation (`format.Clip`), rune-counting, marking the fold — the same reduction the clock surface made when five renderers became three. The marker is `…` rather than `...` deliberately: it is one rune, produced by one function, so the H-10 sweep can say "this document carries a display truncation" as a statement about the SHAPE of a value — and HA's own messages are full of `...`. **(b) within a family that resolves a name, every member resolves it.** `cc logs` resolves through the same `findCustomComponent` `cc show` uses, so a typo is an error and a quiet component is an empty answer, and the two are distinguishable. Refusing rather than hinting is H-22's pole at the boundary, and the family's own name settles the scope question — `cc` is "inspect custom components", so a built-in logger name has never been in range. The cost is one extra `manifest/list` round trip per `cc logs`, which `cc show` already pays. | (a) `format.Table.SetWidth` + `format.Clip` + `displayRows`/`flattenLines` in `internal/format/format.go`, called from `log`, `log --unique`, `cc logs`, `ent ls`, `label ls`, `changes` and `analyze.FormatCondensed`; gates `TestSetWidth_JSONCarriesTheWholeValue`, `TestSetWidth_FullLiftsTheCap`, `TestSetWidth_TextCapsTheColumn` (the control), `TestSetWidth_ACellIsOneLine`, `TestClip_NeverCutsARuneInHalf`, `TestStepOutcomeKeepsTheWholeError`, `TestJSONContract` clause (6) with NO field exemption (`state` is exempt from clause (5) and is exactly where `ent ls` truncated), `TestLogJSON_CarriesTheWholeMessage` + `TestLogText_ComponentIsTheLastSegment` as its control; closure gate `TestTruncationSurfaceIsClosed` over `dev/surfaces/truncation.manifest`; rig shape R6 (`logshapes.py`: a long message, a short first line with more after it, a two-byte character straddling the cut, a traceback) pinned by `TestRigFixtureCarriesTheErrorLogShapes`; sweep cases `TestSweepLogJSONCarriesTheWholeMessage`, `TestSweepLogTextIsOneRowPerLine` (both profiles). (b) `findCustomComponent` in `internal/cmd/cc.go`; `TestSweepCCLogsRefusesAComponentThatIsNotInstalled` (both profiles, with an installed-but-quiet component as the control); the `target` surface's `runCCShow` exemption is deleted, because the resolution an AST analysis could not see is now a call result it can. Laws: H-10, H-22 |
| D-20 | What may a preview refuse on Home Assistant's behalf, and what must it say when it cannot refuse? Live-fire #42/#44/#96: `ent rename` accepted five ids HA answers "Invalid entity ID"/"New entity ID should be same domain" to; `svc call` echoed `--data '{"target":{…}}'` back as a clean plan and --confirm answered 400; and a targetless `svc call light.turn_on` previewed identically to a single-entity call, which the report read as an unflagged domain-wide broadcast. | DECIDED 2026-07-31: **the server owns the rule, in both directions.** A preview refuses exactly what the confirmed run's server refuses — HA's `VALID_ENTITY_ID`, its same-domain rename rule, and the accepted key set its own `GET /api/services` publishes (flattened over UI sections) — and refuses nothing else: an `entity_id` that names nothing, a service that documents no fields, and a section's leaf field are all things HA answers 200 to, so refusing them would be H-2 pointing the other way. Where the answer is legal but not obvious, the preview **states** it rather than refusing: a targeted service with no selector reaches no entity (measured in HA's `helpers/target.py`, not the broadcast the report assumed), `entity_id: all` reaches every entity of the domain. | `internal/haapi/{entityid,servicedata}.go` + `internal/cmd/svc.go`'s `refuseUnusableServiceData`/`serviceCallReach` + `internal/cmd/ent.go`'s rename shape gate; tables carrying both directions in `internal/haapi/servicedata_test.go` and `internal/cmd/ent_rename_test.go`; oracle `TestOracleEntityIDRule` reads HA's own compiled regex out of the running container, so the mirror fails the build rather than a user's rename |
| D-21 | What a confirmed `svc call` may claim about its own effect. `POST /api/services` answers with the state changes HA attributed to the call and hactl discarded the body, so "called input_boolean.toggle", exit 0, was the whole report whether an entity had changed or nothing had matched (live-fire #43). | DECIDED 2026-07-31: **report HA's answer, never upgrade it.** The changed-entity list is printed and carried in `--json` as `changed_entities`; an empty list is reported as "none reported", explicitly NOT as "nothing matched" — measured on a live instance, `automation.trigger` against a real automation also answers `[]`, because the automation's own state change carries a child context. A tool that turned that into "no entities affected" would have made a new false claim out of fixing a silent one. | `haapi.CallService` returns `[]ServiceStateChange` (identity: `entity_id`), rendered by `internal/cmd/svc.go`; `internal/livefire` sweep case on both profiles |
| D-22 | What does "byte preservation" mean for the entry a write actually touches? D-14 settled that a single-entry write leaves every OTHER byte alone, and `auto apply`/`auto rollback` reached that contract on 2026-07-31 by rerouting off Home Assistant's own endpoint (issue #128). The entry being written is a different question: the companion re-serializes it in its own canonical style, so a candidate whose sequences are indented four spaces comes back indented two — once. | DECIDED 2026-07-31: **the contract stays entry-granular, and the preview says when it bites.** Normalizing the edited entry is acceptable (it is idempotent: a second write moves nothing further, pinned by the E2E's second apply+rollback cycle); silently showing a diff that understates what lands is not. The companion's dry-run already diffs its own serialization of both sides, so the preview reports that count as `formatting: N lines change on disk, not M` whenever it exceeds the diff shown — the same signal-already-in-hand lesson as D-21. The manual routes callers to `auto cat > file`, where the two agree exactly. | `writer.ApplyResult.WriterChangedLines` from `PUT /v1/config/automation?dry_run=true`, rendered by `runAutoApply`; `TestE2EAutoApplyWritesOnlyItsOwnEntryCLI` asserts the bystander's bytes, the exact value round trip with no schema fold, and the idempotence of the cycle |
| D-23 | What `dash show` answers about a dashboard whose views are not stored. Two live-fire findings are one question with two states. #3: a dashboard between `dash create` and its first `dash save` — the family's own documented next step — got `fetching dashboard config: lovelace/config failed: No config found.` at exit 1, in plain AND `--json` form, while the identical condition on the DEFAULT dashboard produced a documented report at exit 0. #8: `dash show map --view anything` printed `no views` at exit 0, where the same missing view on a dashboard that has views exits 1. | DECIDED 2026-07-31: **the state is reported, and the question asked is the question answered.** (a) "Home Assistant holds no config" is a state of ANY dashboard, so `classifyDefaultDashboard` becomes `classifyDashboardConfig(url_path)` and every reader goes through it. The report keeps the default's documented `state: "auto-generated"` and gives a named dashboard `state: "no-stored-config"` — two different facts, not two wordings: HA builds the default at view time from areas and devices, and about a named one hactl knows only that HA holds nothing. It deliberately claims nothing about what the frontend renders instead, because it has not asked the frontend. `--raw`/`--yaml` refuse, as they already did for the default: emitting `{}` would be a config asserting the dashboard is empty. (b) **The wire cannot tell "no config" from "no dashboard"** — HA answers `config_not_found` to both, differing only in an English message (`No config found.` vs `Unknown config specified: x`, measured 2026-07-31) — so every caller resolves the url_path against `dashboards/list` first, and `dash replace` gains the resolve it was missing (its `confirm.manifest` debt row). (c) A dashboard HA holds no config for holds no REFERENCES either, so `walkDashboardConfigs` counts it as scanned-and-empty rather than unscanned — this is the sharp half: one unsaved dashboard made `dash grep` call a complete answer partial and made `ref validate`, a gate, refuse to certify anything at all under D-7. (d) `--view` is consulted BEFORE the no-views branch, so a missing view is always exit 1; and zero views with a `strategy` key is reported as strategy-generated, naming the strategy, because "no views" alone reads as "this dashboard is empty" when HA's own onboarding-created `map` dashboard is neither. | `classifyDashboardConfig`, `noStoredConfigAnswer`/`reportNoStoredConfig`, `haapi.LovelaceStrategyType` (internal/cmd/dash.go, internal/haapi/lovelace.go); sweep cases `TestSweepDashShowStatesThatNoConfigIsStored`, `TestSweepADashboardWithNoConfigIsScannedNotSkipped`, `TestSweepDashShowViewNotFoundIsAlwaysAnError`, `TestSweepDashShowNamesTheStrategyThatBuildsTheViews` (all four on both profiles); rig shape R12 (`.storage/lovelace_dashboards`: a registered dashboard with no config, and HA's own `{"strategy":{"type":"map"}}`) pinned by `TestRigFixtureCarriesTheDashboardShapes` + `TestRigServesTheDashboardShapes`; laws: H-10 (D-7), H-22 |
| D-24 | Which question does "the default dashboard is listed" answer? `defaultDashboardIsListed` decided both the scan DEDUP (is the "" pseudo-target the same dashboard as the `lovelace` entry?) and the WRITE GATE (is the default YAML-mode?), on the strength of an oracle captured 2026-07-27 in which the only state producing a listed default was the YAML one. On the reference instance `dash save` and `dash replace` refused every write to the default, citing a `lovelace: mode: yaml` line its configuration.yaml does not contain. | DECIDED 2026-07-31: **split the predicate; the mode comes from the entry's own `mode` field.** Home Assistant 2026.x migrates a stored default into the dashboards collection at boot (`_async_migrate_default_config`, storage mode only), so a listed default is now ordinarily a STORAGE-mode one — the premise was not wrong when it was written and stopped being true in an HA release. The dedup half is unchanged and is now backed by HA's own handler rather than by inference: with no url_path the websocket layer serves `dashboards["lovelace"] or dashboards[None]`, so the two targets are one dashboard in both modes. Only the mode half changes, and it changes to reading the field that carries the answer. The rig learns the shape by seeding the PRE-migration `.storage/lovelace` and letting HA perform the migration itself, so an HA that changes or drops the migration breaks the shape test rather than silently restoring the blind spot. | `defaultDashboardListing` + `refuseYamlModeDashboardWrite` + the `dashboardStorageMode` constant (internal/cmd/dash.go), `planDashboardReplacements` (internal/cmd/ref.go); sweep case `TestSweepDefaultDashboardWriteFollowsItsOwnMode` (both profiles); rig shape R12; law: H-2 |
| D-25 | What may a confirmed write claim when Home Assistant answers an error? `dash delete --confirm` on a long url_path exited 1 with `deleting dashboard: lovelace/dashboards/delete failed: Unknown error` — and the dashboard was gone (live-fire #57). HA removes the item from its collection and then fails unlinking `.storage/lovelace.<id>`, from a listener that runs after the removal, because the path passes the filesystem's 255-byte filename limit (`OSError: [Errno 36]`, traceback captured live). | DECIDED 2026-07-31: **the outcome is a fact about the instance, so ask the instance.** H-12 already said a write claims nothing HA did not confirm; this is the mirror it shipped without — a write HA did not confirm must not claim nothing happened either. After a failed confirmed delete, hactl re-resolves the url_path and reports what is true: gone means `ok: true` at exit 0, with HA's own error carried into `warnings` so the failure is not swallowed on the way; still there means the failure stands. Refusing the create instead was rejected: hactl would have to guess the filesystem's limit and HA's own key derivation, and the guess would refuse working paths on a filesystem with a longer limit. | the read-back in `runDashDelete` (internal/cmd/dash.go); sweep case `TestSweepDashDeleteReportsWhetherTheObjectIsGone` (both profiles — it creates and destroys its own 300-character dashboard, and skips with a stated reason on a filesystem that accepts the path); law: H-12 |
| D-26 | Three flags name `dash show`'s output format (`--raw`, `--yaml`, the global `--json`) and the command honoured whichever its if-statements reached first: `--raw --yaml` printed compact JSON, `--json --yaml` printed YAML, both at exit 0 with no notice (live-fire #60, filed UNVERIFIED and confirmed on the reference instance). | DECIDED 2026-07-31: **refuse the combination.** Noting the winner is not available — under `--json` a note on stdout breaks H-10 clause (1), and a note on stderr is the same silence one stream over — and a documented precedence is a rule every caller has to learn instead of a question the tool answers. Refusal is also the only answer that survives a fourth format. Booleans about the format are distinguished from booleans about the CONTENT: `config file --raw` means "leave `!include` directives unresolved" and composes with `--json` rather than competing with it, so it is exempt with that reason written down rather than absent from the surface. | `checkOutputFormatFlags` + `outputFormatFlagNames` (internal/cmd/outputformat.go), gated in `dashShowCmd`'s RunE because only cobra knows which flags the caller passed; `TestDashShowRefusesConflictingOutputFormats` (with each format alone as the control), closure gate `TestOutputFormatSurfaceIsClosed` over `dev/surfaces/outputformat.manifest`; sweep case `TestSweepDashShowRefusesTwoOutputFormats` (both profiles); law: H-10 |
| D-27 | `dash ls --json` answered `"admin": "true"` — the string (live-fire #59). The mechanism, the fix and the helper all already existed: `format.Table.SetMachine`, documented on `yesNo` in the exact terms needed, used by two commands. Is this one command's omission, and are NUMBERS in the same position? | DECIDED 2026-07-31: **booleans are typed; numbers stay as they are; and the closure is structural, not a wider value list.** (a) Four sites had the defect and the report named the one its author happened to run — `dash ls`'s two columns, plus `restored` on `ent ls` and `auto ls` — so the deliverable is `dev/surfaces/boolcell.manifest`, a census of every place a bool becomes a table cell, keyed per rendered EXPRESSION because `dash ls` renders two and fixing one is the half-fix a per-function key would hide. (b) The value-shaped gate that should have caught it, `TestJSONContract` clause (5), held a vocabulary of `-`/`yes`/`no` — the output of the two renderers the PREVIOUS fix had touched — so `strconv.FormatBool` was outside a check whose own comment refuses enumerations. Its list now covers `true`/`false` too, and it stays the weaker half by construction: `boolCell` renders false as `""`, which no value check can distinguish from an honestly empty string. (c) Numbers are deliberately NOT included. `"runs_24h": "0"` is wrong in the same way and is a documented convention of the table listings (`docs/manual.md`, "Output conventions"), so changing it is a contract change for every consumer rather than a defect fix — and the harm differs in kind: `"false"` is truthy, which inverts a branch, while `"0"` is truthy in a place a caller is far more likely to be comparing or parsing. Recorded here so the asymmetry is a decision rather than an oversight; it is WP10's to revisit with the rest of the flag/format contract. | `SetMachine` at `runDashLs` (two columns), `runEntLs`, `runAutoLs` + the `restoredColumn` constant that keeps a header and its machine key one string; `TestBooleanColumnsRenderAsJSONBooleans` (five commands, each column asserted true AND false); closure gate `TestBoolCellSurfaceIsClosed`; sweep case `TestSweepBooleanColumnsReachJSONAsBooleans` (both profiles); law: H-10 |
| D-28 | What is a `--pattern` glob anchored to, when the identifier a listing prints is qualified? `helper ls --pattern anwesenheit` matched six helpers and `--pattern 'anwesen*'` matched none, because ids are `input_boolean.anwesenheit_flur` and an anchored glob starts at the domain (live-fire #29). | DECIDED 2026-07-31: **a glob is anchored at every identifier form the listing prints** — the id as printed AND the part after the domain. The alternative, documenting the anchor in the flag's help, was rejected because one listing cannot state one anchor: `helper ls` prints a bare YAML slug for a companion-managed row and a full entity_id for a storage-backed one, in the same column, so `--pattern 'pg_*'` matched by which SOURCE a row happened to come from rather than by what the caller asked. Auto-wrapping the pattern in `*…*` was rejected too: it destroys the anchor entirely, and `ent ls --pattern 'sensor.*'` would then also match `binary_sensor.*`. The dot keeps its selecting power — the tail of `binary_sensor.foo` is `foo`, which `sensor.*` does not match. A value with a dot that is not a domain (a device named "Sonos v1.2") gains an unused second anchor, which is a false positive in the permissive direction and cannot turn a match into a miss. | `matchPattern` (`internal/cmd/ent.go`), shared by every `--pattern` in the tree; `TestGlobIsAnchoredAtEveryIdentifierFormTheListingPrints` quantifies over the live tree's glob-documented filters, not over the command that reported it; sweep case `TestSweepGlobMatchesTheNameNotOnlyTheQualifiedID`; law: H-17 |
| D-29 | What does a listing say when it has nothing to show, and what may that answer imply? `helper ls --pattern zzz` printed `no helpers` on an instance holding 220 (live-fire #28); `device ls` said `no devices` on one holding 307; four more printed a bare table header. | DECIDED 2026-07-31: **an empty listing states what narrowed it, and how much it searched.** `no helpers match --pattern "zzz" (220 helpers on this instance, none matching)`; the plain noun only when nothing narrowed and the inventory really is empty. The count is part of the decision, not decoration: it is the one fact the caller cannot get from the message, and running the unfiltered listing to learn it is exactly what the false answer stopped them from doing. The bare header was rejected as the pole even though it is honest — it is byte-identical to the answer on an instance that holds nothing, so the two cases a caller most needs to tell apart looked the same. Under `--json` the answer stays `[]` (H-10): the reason is prose for a reader. A narrowing hactl forwards rather than applies (`companion logs`) names the narrowings and omits the count rather than inventing one. A SELECTOR refuses instead of answering empty — `dash show --view x` exits 1 — and owes the same naming (D-23/#8). | `emptyListing`/`activeNarrowings` (`internal/cmd/listing.go`), reading the narrowings off the command's own flag set so no call site enumerates them; eight listings rewired; closure gate `TestEveryNarrowedListingSaysWhatNarrowedIt` walks the live tree and drives every narrowing flag it finds; `TestEmptyListingCountsTheInventoryItSearched`, `TestEmptyListingUnderJSONIsStillAnArray`; sweep case `TestSweepEmptyListingNamesItsFilter`; law: H-10 |
| D-30 | Which questions does `helper create` owe an answer BEFORE it prints a plan? The C-10 wiring probe closed live-fire #63 as reported, and `helper create script` still previewed a create the companion answers 400 to; nothing anywhere checked the id (live-fire #64). | DECIDED 2026-07-31: **three questions, in that order, and the two local ones run first.** (1) Is the domain one `helper create` writes? — the wiring probe answers a DIFFERENT question ("does HA read this domain's config from a file this route can extend"), which is true of `script`, `automation` and `template`, so a local set is required and `helperCreateDomains` is it, held to the companion's ALLOWED_DOMAINS by `TestE2EHelperCreateDomainSetMatchesTheCompanionCLI`. (2) Is the id one Home Assistant can turn into an entity? — `haapi.ValidEntityID(domain + "." + id)`, because the id IS the object_id the create mints. (3) Does HA read this domain's file? — the remote probe, last, because it costs a round trip. Being stricter than the companion is the point of (2): the companion writes whatever key it is handed, so preview and confirm agreed perfectly on a create that poisons the file — HA validates a helper file with `cv.schema_with_slug_keys`, which fails the WHOLE mapping on one bad key and takes every working helper in that shared file down with it. hactl's rule and `cv.slug` differ on exactly one input, the empty string, and `TestOracleHelperIDRule` asserts that this stays the only difference. | `validHelperCreateDomain`, `validHelperCreateID` (`internal/cmd/helper.go`); `TestHelperCreateRefusesADomainItCannotWrite`, `TestHelperCreateRefusesAnIDHomeAssistantCannotUse`, `TestHelperCreateAcceptsTheIDsHomeAssistantAccepts` (the control); oracles `TestOracleHelperIDRule` (`make test-int`) and `TestE2EHelperCreateDomainSetMatchesTheCompanionCLI` (`make test-companion`); sweep case `TestSweepHelperCreateRefusesWhatConfirmRefuses`; law: H-2 |
| D-31 | What does a SEARCH command owe when a source of its sweep went unread? D-7 answered this for `ref validate` and exempted `ref scan`/`dash grep` in one clause of prose: they "warn and still answer — and their `--json` shape does not change, because a scope note on stdout would break clause (1) for them". Live-fire #34: with the companion timing out, `ref scan <id> --json` returned 3 of 24 references at exit 0, and for an id whose references are all in config files it returned `[]` — a verified negative for a query that never ran. | DECIDED 2026-07-31: **the medium decides, and the exemption was never about the rule.** A search command still answers over what it could read, and states the scope in the REPORT BODY (stdout), because a `slog.Warn` is invisible to anyone whose `HACTL_LOG_LEVEL` hides it and to every machine. Under `--json` the document is a bare array of rows with nowhere to carry the caveat, so the command REFUSES and prints nothing, exactly as `ref validate` does — `--allow-partial` is the acknowledgement, on the same flag name, and `ref scan` and `dash grep` both gained it. An empty answer may never claim the negative: "not referenced" becomes "no reference in what could be scanned". **And the config half is a source, not a transport**: the companion answers 200 with a `skipped` array naming files its walk could not read, hactl has decoded it on three response types since the v2026.7.9 spec sync, and nothing read it — the limit D-7 recorded in writing. A 200 that skipped a file is now partial for every consumer: `ref scan`, `ref validate` (gate), `ref replace` (refuses in dry run; a confirmed run reports what it could not read rather than claiming it renamed everything), and `ent rename` (whose reference COUNT was the number a caller reads before deciding to `--confirm`). | one `sweepScope` carrying every source + `reportSweepScope` + `searchScopeGateError` beside the existing `validateScanGateError` (internal/cmd/ref.go); `TestRunRefScan_PartialRefusesUnderJSONUnlessAllowPartial`, `TestRunRefScan_APartialScanNeverClaimsNotReferenced`, `TestRunRefScan_SkippedConfigFilesMakeTheAnswerPartial`, `TestRunRefValidate_SkippedConfigFilesTakeTheGate`, `TestRefReplaceRefusesWhenTheCompanionSkippedAFile`, `TestEntRenameRefusesWhenTheConfigHalfSkippedAFile`, `TestRunDashGrep_PartialIsStatedAndRefusesUnderJSON`; sweep cases `TestSweepAPartialScanSaysSoOrRefuses`, `TestSweepAScanThatSkippedFilesIsPartialToo` (both profiles); quantified by `TestPartialScopeSurfaceIsClosed` over `dev/surfaces/partialscope.manifest`; laws: H-10 |
| D-32 | What may `ref scan`/`ref replace` be asked to match? Live-fire #37 filed `ref scan .` returning 2739 near-meaningless hits as a P3 footgun. Re-measured: 2747 hits, and `ref replace . X` plans **2747 rewrites of real config files**, one `--confirm` from mass corruption. | DECIDED 2026-07-31: **enforce the matcher's own stated precondition, at the client.** The companion builds `\b` + `re.escape(target)` + `\b`, and its docstring says why that means "whole token": *every entity_id-shaped target starts and ends on a word character*. When it does not, each `\b` binds to the neighbouring character and the pattern asks the opposite question — `\b\.\b` matches a dot that HAS word characters on both sides, i.e. the interior of every id and every service name. So the rule is exactly that premise (first and last rune only: "Wozi TV" and "Küche" are legitimate targets, `.turn_on` and `'sensor.x'` are not), it lives in `internal/companion` where every present and future caller of the routes passes through it, and the commands call the same function early so the refusal costs no connection. Both sides of a replace take it: the old value is the pattern, the new value is what lands in every position the id held. A refused target is the caller's mistake and carries its own error type — never absorbed into "the config half could not be read". | `companion.ValidRefTarget`/`RefTargetError`/`InvalidRefTargetError` (internal/companion/reftarget.go), called by `RefScan`/`RefReplace` and by `runRefScan`/`refReplaceWithOptions`; `TestRefRoutesRefuseATargetThatIsNotAWholeToken`, `TestValidRefTargetMatchesTheCompanionsPremise`, `TestRefScanRefusesADegenerateTargetAsAnErrorNotAPartialAnswer`; sweep case `TestSweepRefScanRefusesATargetItCannotMatch` (both profiles) |
| D-33 | May an error erase output the command already produced? `Execute()` rendered into a buffer and flushed it only when `RunE` returned nil, so `ref validate --exit-code` — which renders the dangling-reference table and then returns a sentinel error carrying the exit code — printed **zero bytes of stdout** on a real instance with 429 dangling references. Its unit test asserted the report reaches the writer, and it does; `RunWithOutputContext` (MCP, integration tier) flushed on both paths, so no in-process tier could see it. | DECIDED 2026-07-31: **an error ENDS a command; it does not ERASE what the command printed.** The buffer is flushed on both paths, so a verdict keeps its report and `ref replace`'s two post-report refusals keep the plan whose own comments say it is rendered "so the caller sees what is stuck". The manual's promise for bad input — exit 1, stderr, empty stdout — is untouched and was never the entry point's doing: a refusal refuses BEFORE it renders, so there is nothing to flush. Verified in both directions rather than assumed. | the flush in `Execute()` (internal/cmd/root.go); `TestExecutePrintsTheAnswerItRenderedBeforeAVerdictError` and its control `TestExecutePrintsNothingWhenTheCommandRefusesBeforeRendering` (driven through the real entry point with os.Stdout redirected — the only place in-process that can see it); sweep case `TestSweepRefValidateExitCodeAnswersOnStdout` |
| D-34 | What does `--timeout` bound? It is documented as "per-request timeout for HA/companion API calls" and the REST client obeyed it, but live-fire #73: `companion status --timeout 1s` against a host that never answers returned after **10.02s**, with `--timeout 3s` and `--timeout 20s` landing on the same 10.02s, while `health --timeout 1s` and `ent ls --timeout 1s` against the identical host aborted at 1.01s. | DECIDED 2026-08-01: **every connection, including a retry the caller did not ask for.** The wording in the manual was never wrong — the WebSocket transport simply read constants (a 5s dial, attempted twice, behind a 10s handshake) — so this is the code catching up with the documentation rather than a change of contract, and `docs/manual.md` is unchanged. Two consequences follow from "every": `WSClient.Connect`'s retry runs INSIDE the budget and is skipped once the budget is gone, because two attempts do not entitle a command to twice the time the caller allowed; and `DialTimeout` stays a CEILING on the wait rather than a floor under it, so an unreachable host still fails in ~5s at the 30s default while `--timeout 1s` gets 1s. The three transports were three files by three changes, which is why one of them could ignore the flag invisibly — so they are now one constructor, and the set of transports is a closure gate rather than a thing to remember. | `haapi.HTTPClient`, `dialBound`, `handshakeBound`, `RedirectError` (internal/haapi/transport.go), consumed by `haapi.New`, `haapi.WSClient.connect` and `companion.New`; `TestWSConnectHonoursTheCallerTimeout`, `TestHTTPClientIsBoundedByTheCallerTimeout`; quantified by `TestTransportSurfaceIsClosed` over `dev/surfaces/transport.manifest`; sweep case `TestSweepEveryTransportHonoursTheCallerTimeout` (both profiles); law: H-23 |
| D-35 | What may a redirect do to hactl's answer? Live-fire #76: with `HA_URL=http://…` behind a proxy that 301s to `https://`, `health --json` returned a real version, state, location and timezone beside `"errors": -1` and `"companion_status": "not found (unreachable)"`, at exit 0 — and `ent ls` returned all 4 486 real entities. | DECIDED 2026-08-01: **hactl talks to the URL it was configured with; a cross-origin redirect is reported, not followed.** The asymmetry is structural and not fixable by making the WebSocket smarter: an HTTP client follows a 301 transparently with the credentials intact, and the WebSocket protocol has no redirect step at all, so following it can only ever produce a half-working instance — REST commands answering from the real house while traces, registries, add-on discovery and the recorder error count fail for a reason nothing names. Both transports now detect the same condition and report it in the same words: the REST client through a `CheckRedirect` that refuses when scheme, host or port change, and the WebSocket by READING the 3xx response gorilla hands back beside `ErrBadHandshake` — the Location header was always there and the call site assigned it to `_`. Same-origin redirects stay followed to the standard library's own hop limit, because a trailing-slash redirect is a server tidying its URL space and Ingress is served under a path prefix. | `haapi.RedirectError`, `originGuard`, `redirectFromHandshake`, `Origin` (internal/haapi/transport.go); `companion.ReasonRedirected`; `TestRESTRefusesARedirectThatMovesTheOrigin`, `TestWSConnectReportsARedirectAsTheSchemeProblemItIs`, boundary `TestRESTFollowsARedirectWithinTheSameOrigin`; sweep case `TestSweepARedirectedOriginIsNamedNotFollowed` (both profiles); law: H-23 |
| D-36 | What does a connectivity diagnostic owe its caller? `companion status` printed `WS connect: failed (authentication failed: Invalid access token or password)` and, one line below, `discovery: failed (unreachable)` with the hint "Check Ingress / network, or set COMPANION_URL in .env" — and exited **0**. A rejected token, a refused port and a blackholed host produced identical text and identical exit codes (live-fire #74, #75). | DECIDED 2026-08-01: **the cause is the one the transport reported, and the exit code carries the verdict.** The wrong label was not a wording slip: the discovery reason was computed from a NIL WebSocket client — the absence of a connection — so by construction it could not say more than "something is missing", while the error that produced the absence was sitting in a field two lines up. `WSClient` now remembers its `ConnectError()`, `Discover` is handed the client connected or not and classifies from that error, and the classification is by TYPE (`*haapi.AuthError`, `*haapi.RedirectError`) rather than by substring. Two reasons were added because three causes had been sharing one label; each names its own remediation, and `newDiscoveryError` quotes the cause instead of paraphrasing it. The exit code follows the command's SUBJECT: discovery or health failing is exit 1, while a WebSocket that could not open beside a companion reached directly through `COMPANION_URL` is not — the companion is usable and the body says what else is not. The report is rendered before the verdict is returned (D-33). | `haapi.WSClient.ConnectError`/`Connected`, `haapi.AuthError`; `companion.classifyConnectError`, `ReasonAuthInvalid`, `ReasonRedirected`, `DiscoveryReasons()`; `companionUnreachableError` (internal/cmd/companion_cmd.go); `TestCompanionStatusExitsNonZeroWhenTheCompanionIsNotUsable` + its control, `TestCompanionStatusNamesTheCauseTheTransportReported`, `TestClassifyConnectError`; quantified over the reason set by `TestDiscoveryReasonsMatchTheConstBlock`, `TestEveryDiscoveryReasonNamesItsOwnFix` and `TestEveryDiscoveryReasonHasAStatusLine`; sweep cases `TestSweepCompanionStatusVerdictReachesTheExitCode`, `TestSweepCompanionStatusNamesTheCause` (both profiles); law: H-24 |
| D-37 | What may `trace show` be handed? The manual's automation paragraph names it in the D-1 identifier contract — "`auto show\|cat\|diff\|apply\|delete\|rollback`, `trace show` — accepts any of its interchangeable names" — and it answered all four forms with `invalid trace ID format` (live-fire #66). | DECIDED 2026-08-01: **implement the promise, do not withdraw it.** A trace address names a RUN (`trc:<hash>`, `domain.item_id/run_id`); anything in neither form is not malformed, it is an automation reference, and the answer is that automation's most recent stored run — which is what "why did X fail?" means and what a caller previously had to reach through `auto show` and copy a `trc:` id for. The resolution goes through `resolveAutomation`, so the command inherits the contract rather than re-deriving a narrower version of it. Two failures stay distinct because the fix differs: a reference naming no automation is the caller's mistake, and an automation Home Assistant holds no trace for is an instance fact. **And the gate is widened, not just the command**: the `autoref` surface derived membership from the entrypoint's PARAMETER NAME, so `traceID` hid the one command the manual names outside the `auto` family; it now also reads the manual's sentence and holds every command it lists to the resolver. | `errNotATraceReference`, `latestAutomationRun` (internal/cmd/trace.go); `commandsPromisedTheIdentifierContract` in `AutomationRefSurface` (internal/surfaceaudit/scan.go); `TestResolveTraceIDSeparatesAddressFromReference`, `TestResolveTraceID_NeitherAddressForm` (the old `…_InvalidFormat`, renamed because the name was the defect); sweep case `TestSweepTraceShowAcceptsEveryAutomationIdentifier` (both profiles, identifiers read from what `auto show` prints); laws: H-17, D-1 |
| D-38 | What shape does a rendered time column have? `auto show`'s trace table printed `07-29 01:15` on four rows and a bare `01:15` on the fifth, whose run started today (live-fire #71) — an undocumented convenience that a reader cannot date and an LLM consumer of the text output cannot interpret. | DECIDED 2026-08-01: **the abbreviation is a property of the COLUMN, not of the value.** "Today means no date" is a relative rendering, and a relative rendering is legible only where its reference point is visible: in a scalar (`last_changed: 06:31`) it is, in a column it is not. So a column is bare `15:04` only when EVERY value in it falls on the reader's today, and `01-02 15:04` as soon as one does not. The decision is enforced where SetWidth's is, and for the same reason its doc comment gives: every renderer used to turn the instant into a clock while ASSEMBLING the row, so by the time the table could see the column the date was gone and there was nothing left to undo. The cell now carries the wire value, `format.Table.SetTimeColumn` declares the column, and the shape is decided at display time — after `--top` has chosen the rows, because a date that appears only because of a row nobody sees is a worse answer than the one it replaced. `--json` is untouched: it already carried the full instant (#85). | `clock.ShortColumn` (internal/clock/render.go), `format.Table.SetTimeColumn`/`renderTimeColumns` (internal/format/format.go), called by `auto show`, `changes`, `ent ls`, `ent hist` and `ent anomalies`; `TestShortColumnIsUniformDownTheColumn`, `TestShortColumnPassesThroughWhatItCannotParse`; the site is dispositioned on the clock surface (`dev/surfaces/clock.manifest`); sweep case `TestSweepATimeColumnHasOneShape` (both profiles); law: H-20 |
| D-39 | What does a rendered schema owe a caller? `config flow-inspect` typed every selector-backed field as `string` — a template, a 28-value enum, an entity picker and a device picker alike — and left the Default column empty beside a field whose current value HA had sent (live-fire #82, #83). | DECIDED 2026-08-01: **read the selector, and read the suggestion.** A modern Home Assistant types a form field with `"selector": {"<kind>": {…}}` and leaves `type` empty; the kind is the single key of that object, so it is READ rather than matched against a list — a selector HA adds next month types its field correctly with nobody touching the parser, and a hand-list is what made all of them "string". `type` still wins where a field declares one (`expandable` does), and "string" remains the answer when HA said neither, which is what an unadorned field is. A select selector's `options` are the submittable values and join the list the renderer already prints, so the contrast the report drew — a MENU step showing its choices while a 28-value enum showed none — closes with no new machinery. And `description.suggested_value` — the entry's current configuration, the one thing an options flow carries that a fresh config flow does not — now fills the Default column, ahead of `default`, because that is what a caller re-submitting a form needs. | `haapi.SchemaField.Selector`/`Suggested`, `parseSelector` (internal/haapi/flow.go); `schemaFieldType`, `schemaFieldDefault` (internal/cmd/flow.go); `TestSchemaTableNamesTheSelectorKind`, `TestSchemaTableShowsTheValueHASuggests`, `TestSchemaFieldDefaultPrefersTheSuggestion`, `TestSchemaFieldTypePrefersTheFieldsOwnDeclaration`; sweep case `TestSweepFlowInspectTypesItsFields` (both profiles, on a config entry the rig now builds — capability R7); law: H-13 |
| D-40 | Where does the explanation of a failed flow lookup live? `config flow-step <bad-id>` said "flows expire; start one with 'config flow-start' or 'config options'"; `config flow-inspect` on the same id handed back HA's bare 404, and `flow-step --confirm` a third variant (live-fire #84). | DECIDED 2026-08-01: **at the condition, not at the command.** One helper wraps every flow-id failure, so the three commands that address a flow by id cannot drift apart again, and it names the three causes Home Assistant answers identically — the id never existed, the flow expired or was aborted, or it belongs to the other endpoint. The third is the one a caller cannot guess from a 404, so the message names the flag and the direction to move it (`add --options` / `drop --options`). Anything that is not a 404 passes through untouched: a 500 is not a caller mistake and telling them to check their flag would be a guess. | `flowLookupError` (internal/cmd/flow.go), called from `runConfigFlowStep` (both branches) and `runConfigFlowInspect`; `TestFlowLookupErrorExplainsThe404Everywhere` including the 500 and transport boundaries; sweep case `TestSweepFlowSiblingsExplainTheSame404` (both profiles) |
| D-41 | What does hactl owe a flag the caller typed? Nine live-fire findings are one missing rule. `--since` was a root persistent flag on all 112 commands and read by nine, so `area ls --since garbage-value-xyz` exited 0 with output byte-identical to `area ls` while `log` refused the identical value (#54). `--top -1`, `--top 0`, `--tokensmax -5` and `--timeout 0s` were each reinterpreted rather than refused (#47, #53, #56). `rtfm --json` printed Markdown while the manual's list of commands `--json` does not reach failed to name it and did name `tpl eval`, which honours it (#12). `hactl --version --json` printed the plain banner while `hactl version --json` printed JSON (#13). `tpl eval "{{ 1+1 }}" -f file` evaluated the file and discarded the argument (#6). `ent ls --tpo` got four words where `hactl ento ls` gets cobra's "Did you mean this?" (#48). And `--json` suppressed the progressive manual for read commands while `confirmGuard` delivered it for writes (#50). | DECIDED 2026-08-01 (Jan): **a flag hactl offers is a flag hactl honours** — H-25, H-22's other half. Three clauses. **(a) Reach.** A flag is declared on the commands that act on it; there is no third state between honouring one and refusing one, and documenting the gap is not the fix. So `--since` moved off the root onto its nine readers, which is structural rather than a gate: cobra refuses an undeclared flag by itself, `area ls --help` stops advertising an effect that does not exist, and there is no "commands exempt from --since" registry to fall out of step with the code. The declaration set is PROVEN to be the consumption set — `sinceWindow` is the only read of the flag and records that it happened, so a command that declares it and never looks at it fails the build. `--json`'s reach is stdout, and the manual has never been on stdout, so `--json` stopped deciding manual delivery; on a real instance it had cost `health --json` its entire 10 262-byte core delivery while `area create --confirm --json` got 12 447 bytes from the guard, which is hactl disagreeing with itself in writing. **(b) Domain.** A cap and a bound are different promises: `--top 0` and `--tokensmax 0` mean "no cap" and now say so in both help texts, negatives are refused, and `--timeout` refuses zero and below — H-23 says every connection is bounded by the caller's `--timeout` and a bound of zero makes that law vacuous, while a negative reached `net.Dialer` as a deadline already in the past and came back as `dial tcp: lookup <host>: i/o timeout` against a host that was up. The check runs in `PersistentPreRunE`, before the value is installed, so a flag value can never become a network diagnosis. **(c) Exclusivity.** Two inputs naming one thing ends the command, D-26's rule one syntax over; `tpl eval` is the only site in the tree where a positional and a flag name the same input, and its unit test had pinned the silent precedence as correct. The corollary the reach clause earns is that a refusal owes an address: `unknown flag: --since` alone makes the next move a guess, so the error names the commands that do declare it, and a near miss is answered with the flag the caller meant. **`--top` was deliberately NOT moved** with `--since`, and the surface says so rather than leaving it implied: it is inert on every command that renders no table, moving it would take `--full` with it, and a debt row a reviewer reads is the honest form of a change not made. | `internal/cmd/flagcontract.go` (`globalFlagDomains`, `checkGlobalFlagDomains`, `flagErrorHelp`, `commandsDeclaringFlag`, `nearestFlagNames`), `sinceCommands`/`sinceWindow` (internal/cmd/since.go), the root's `PersistentPreRunE` and its own `--version` (cobra answers its built-in from a template, which cannot read a flag); `TestSinceIsDeclaredOnlyOnTheCommandsThatReadIt`, `TestEveryCommandDeclaringSinceReadsIt` (with a command that must NOT read it as the control), `TestACommandThatCannotActOnSinceRefusesItAndSaysWhereItLives` (every other command in the tree), `TestGlobalFlagDomainsRefuseWhatTheyCannotHonour`, `TestATimeoutThatCannotBoundNeverReachesATransport`, `TestUnknownFlagOffersTheNearestFlagTheCommandTakes`, `TestUnknownFlagNameReadsPflagsOwnErrors` (both forms built by pflag, not written as literals), `TestVersionFlagAndVersionCommandAgreeInBothModes`, `TestManualNamesTheCommandsJSONDoesNotReach` (runs every command the sentence names), `TestResolveTemplate_RefusesTwoTemplates` (replacing `TestResolveTemplate_FilePriority`, which asserted the defect), `TestManualDeliveryIsNotDecidedByOutputFormat`; closure gates `TestEveryNumericGlobalFlagStatesItsDomain` and `TestFlagContractSurfaceIsClosed` over `dev/surfaces/flagcontract.manifest`; sweep cases `TestSweepAFlagACommandCannotActOnIsRefused`, `TestSweepABoundThatCannotBoundIsRefused`, `TestSweepAMistypedFlagOffersTheNearestOne`, `TestSweepTwoSpellingsOfTheVersionAnswerTheSame`, `TestSweepTplEvalRefusesTwoTemplates`, `TestSweepTheManualReachesAJSONCaller` (all six on both profiles); law: H-25, and H-23 for the timeout clause |
| D-42 | What does hactl owe a caller it cannot see? Three live-fire findings are one missing premise. Two `auto apply --confirm` calls to one automation, launched together, both printed their own diff, both printed `applied` and `reload: ok`, both exited 0 — and one of the two edits was not in the file afterwards (#100). Both of them logged `backup created` at the same one-second path and one file survived, each caller having been told that path held its recovery point (#101). And `confirmGuard`, the refusal that is supposed to stop an uninformed write, was answered out of `<instance>/cache/manual-state.json` under the session key `default` — which every process on the machine shares — so a process running `area ls` under `HACTL_MANUAL_MODE=full` switched the guard off for a *different* process whose first-ever automation command then wrote to the live instance with nothing on stderr (#61). | DECIDED 2026-08-01 (Jan): **hactl is never the only caller** — H-26. The instance directory is shared state, not private, and the three clauses are the three ways a shared thing goes wrong. **(a) Held.** A confirmed write takes the instance's exclusive lock for as long as it runs. Serialising does not stop the second write from replacing the first — two sequential applies do that too, and it is what the caller asked for; what it stops is a write being planned against a state that no longer exists, so the second writer's backup now holds the first writer's result and `auto rollback` can still reach the version that was replaced. hactl's own read-modify-write window measured 2–3 s live; the companion's is milliseconds and was never the interesting half. **(b) Witnessed.** A `--confirm` is authorized by a dry-run of the SAME command and target, recorded in the instance cache with a 30-minute window. Jan chose this over deriving a per-caller session key from the process ancestry (measured: pid and ppid change per invocation, only the harness grandparent is stable, and no fixed depth is universally right — an unstable key re-delivers the whole manual on every call, a large silent token cost). A delivery record was never evidence about the caller; it is evidence about the directory. It was also weaker than it read: `auto ls` delivers the automation how-to, so `auto ls` then `auto apply --confirm` satisfied a guard whose entire subject is writes nobody previewed. The key is session + command + target, and the session half is not decoration: the live sweep found two sibling cases previewing one automation authorizing a third case whose entire precondition was that nothing had — #61 rebuilt two levels down, found by running the corpus rather than by reasoning. The residue is stated rather than hidden: callers that leave `HACTL_SESSION` unset share the key `default` and therefore share their previews target by target, which is the same knob the manual already documents. **(c) Unique.** A file hactl writes to preserve a state it is about to replace never overwrites an existing one: microsecond stamps plus `O_EXCL`, and a name that cannot be found free FAILS rather than truncating. All three backup writers had the same defect and each would have been fixed alone where it was reported, so they were collapsed into one writer with one proof. **What the law does not claim:** a writer that is not this hactl — HA's own UI, a second machine, a hactl on another directory with the same URL — is not excluded by a local lock; H-12's read-back is what catches everybody else. | `internal/instancelock` (flock / LockFileEx, bounded by the caller's `--timeout`), acquired in the root's `PersistentPreRunE` and idempotent per instance because flock is per open file description; `internal/backupfile` (one writer for `writer.backup`, `backupScriptConfig`, `writeDashboardSnapshot`); `internal/cmd/witness.go` + the rewritten `confirmGuard`; `TestLockIsExclusiveAcrossProcesses` (asks `flock(1)`, not hactl's own implementation), `TestAcquireGivesUpAndSaysWhoHasIt`, `TestWriteNeverOverwritesAnExistingBackup` (clock frozen, so the collision happens by construction), `TestWriteDoesNotClobberAFileItDidNotWrite`, `TestExecute_ConfirmGuardIsNotSatisfiedByAFamilyRead`, `TestExecute_ConfirmGuardRefusalDoesNotAuthorizeTheRetry`, `TestWitnessKeyDistinguishesCommandAndTarget`; closure gate `TestSharedStateSurfaceIsClosed` over `dev/surfaces/sharedstate.manifest`, whose first run found a fourth site of the same lost update in `pkg/ids.Registry.Save` and recorded it as debt rather than fixing it in a hurry; rig capability R8 (`Target.WriteConcurrently`) and sweep cases `TestSweepConcurrentAppliesDoNotShareAStaleRead`, `TestSweepAConfirmedWriteNeverOverwritesABackup`, `TestSweepAConfirmRequiresItsOwnDryRun` (live profile; R11 keeps the companion-routed family off the rig) plus `TestSweepConcurrentDashWritesEachKeepTheirOwnSnapshot` on BOTH profiles over a companion-free write path; law: H-26 |
