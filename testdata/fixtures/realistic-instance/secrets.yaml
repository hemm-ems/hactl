# Referenced from configuration.yaml as `Authorization: !secret shape_api_token`.
#
# The value is nonsense and is never sent anywhere: the point is that the
# document HA loads contains a tag, not a string, and that a tool which
# re-serialises that document has to preserve the difference.
shape_api_token: not-a-real-token-0000
